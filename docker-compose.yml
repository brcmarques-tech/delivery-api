services:
  app:
    build: .
    container_name: delivery-app
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USERNAME: delivery
      DB_PASSWORD: delivery123
      DB_DATABASE: delivery_db
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET: dev-secret-change-in-production
      JWT_EXPIRATION: 7d
      PORT: 3000
      MAIL_HOST: smtp.gmail.com
      MAIL_PORT: 587
      MAIL_USER: brunomarquesag331@gmail.com
      MAIL_PASS: orxxlexfidnpdlew
      MAIL_FROM: bcmTech <brunomarquesag331@gmail.com>
      CLOUDINARY_CLOUD_NAME: dgbavxge6
      CLOUDINARY_API_KEY: 279355333423469
      CLOUDINARY_API_SECRET: lILGnp653qtQQmOGV0uQVDn4Uew
      MP_ACCESS_TOKEN: TEST-4586379795003560-030710-cbc37968a853e072a69d34f816fa83c6-3249886923
      MP_PUBLIC_KEY: TEST-ce1148e7-0e18-4b3b-8220-4a34a8da42b6
    depends_on:
      - postgres
      - redis

  postgres:
    image: postgres:16-alpine
    container_name: delivery-postgres
    environment:
      POSTGRES_USER: delivery
      POSTGRES_PASSWORD: delivery123
      POSTGRES_DB: delivery_db
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: delivery-redis
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
